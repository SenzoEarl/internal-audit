# File: `.env` (example, DO NOT commit this file)
# SECRET_KEY=change_me_to_a_secure_value
# DEBUG=False
# ALLOWED_HOSTS=yourdomain.com
# DATABASE_URL=postgres://audituser:auditpass@db:5432/auditdb

# Notes (apply manually):
# - Create `.`env` locally with secrets and add it to `.gitignore`.
# - Ensure `STATIC_ROOT` exists (already present in your settings).
# - Run `docker-compose build` and `docker-compose up -d` to start.
# - For GitHub Actions, ensure tests don't require secrets; use test DB config via DATABASE_URL.