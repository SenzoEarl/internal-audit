{
  "name": "internal-audit",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild static/js/main.js --bundle --minify --outfile=staticfiles/js/bundle.js --target=es2015"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}